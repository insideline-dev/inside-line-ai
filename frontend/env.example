# Local Vite: full backend URL (vite.config proxies /api → backend when using /api + rewrite).
VITE_API_BASE_URL=http://localhost:8080
# Docker image (nginx): omit or set VITE_API_BASE_URL=/api so the browser never calls a separate API domain.
VITE_FRONTEND_BASE_URL=http://localhost:3030

