{
  "name": "inside-line",
  "version": "1.0.0",
  "private": true,
  "packageManager": "bun@1.3.1",
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"bgBlue.bold,bgMagenta.bold\" \"bun --cwd backend dev\" \"bun --cwd frontend dev\"",
    "dev:backend": "bun --cwd backend dev",
    "dev:frontend": "bun --cwd frontend dev",
    "build": "bun run build:backend && bun run build:frontend",
    "build:backend": "bun --cwd backend run build",
    "build:frontend": "bun --cwd frontend run build",
    "lint": "bun run lint:backend && bun run lint:frontend",
    "lint:backend": "bun --cwd backend run lint",
    "lint:frontend": "bunx tsc --noEmit -p frontend/tsconfig.json",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:build": "docker compose build",
    "docker:logs": "docker compose logs -f"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
