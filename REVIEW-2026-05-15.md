# Review Doc — Deal Screening / DD Refactor (Session 2026-05-15)

> What I shipped, what to review, what I skipped and why, what blocked me, what to verify next.
> Goal context: the plan at `/home/yusuf/.claude/plans/some-notes-from-jaunty-pine.md`.

## TL;DR

4 commits on branch `dev`, ahead of `origin/dev`:

| Commit | Scope | Behaviour change? |
|--------|-------|-------------------|
| `d7a95eb` | PR2 — `ai/primitives` namespace (docs + index re-exports) | None |
| `c0239a4` | PR5 — thesis-fit agent + schema + prompt + 8 unit tests | None (not yet wired) |
| `e3f75bf` | PR6 — stage tabs UI shell + Screening triage queue route | New UI route, placeholder data |
| `492449f` | PR7 — Screening detail modal + local PASS/ADVANCE | New UI flow, local state only |

**Backend:** `bunx tsc --noEmit` ✅, `bun test src/modules/ai/agents/thesis-fit` 8/8 ✅
**Frontend:** `bunx tsc --noEmit` ✅, `bun run build` ✅, browser-verified ✅
**Skipped:** PR1 (deemed harmful — see §"Decisions"), PR3, PR4, PR8, PR9 — reasons below.

## What changed in the original plan (decisions I made)

You will see these because you read the plan with me; make sure you agree before merging.

1. **PR1 dropped.** The "screening → DD" rename PR turned out to be a 30-50 file change involving DB enum values, Redis queue names, OpenAPI codegen, and a 45-call-site `PipelinePhase.SCREENING` rename. On inspection most "screening" naming in the existing code is actually correct (it refers to the real triage/gate concept). I asked you mid-session, you confirmed: drop PR1. Plan file updated to mark it `~~PR1~~ DROPPED`.

2. **PR2 reduced to docs + index re-exports.** The plan called for "lifting extraction/enrichment/scraping out of `processors/` into `primitives/`". On inspection those services already exist as 1500–3500-line, primitive-shaped modules under `services/`. Moving them would be a 6900-line zero-behavior PR — the opposite of "small, verifiable." Instead PR2 establishes the `primitives/` folder with a README documenting the policy and an `index.ts` that re-exports the five services under primitive names (`ExtractionPrimitive`, `GapFillPrimitive`, etc.). When future PRs need stage-aware shape (depth options, parameterised required-schemas) the wrappers will land here.

3. **PR3, PR4, PR8, PR9 deferred to a future session.** Each is high-churn and stops being verifiable inside one session — I describe the dependency chain in §"Why I stopped here".

4. **PR5 not wired into any pipeline yet.** The thesis-fit agent ships with the schema, the prompt, the service, an 8-test spec, and an `AiModule` registration — but no call site. Wiring it into the DD pipeline (and the future screening pipeline) is a follow-up. This is intentional: the schema + prompt are the architectural artifacts you need to review *before* I plug them in across the codebase.

5. **PR6 + PR7 ship with placeholder data** because the backend screening pipeline doesn't exist yet. The placeholder banner is on-screen and the seed rows include the California/US regression case (Acme AI showing geography `match` despite being labelled California). The PASS / ADVANCE buttons mutate React state only — they do not call any backend.

## What to review, in order

### 1. The plan file (5 min)
`/home/yusuf/.claude/plans/some-notes-from-jaunty-pine.md`

This is the single source of truth for the refactor. The PR1-dropped note and the PR2 minimisation are in it. Read §1 (domain model), §2 (screening pipeline), §3 (primitives), §4 (thesis-fit), §6 (UI), §7 (smart re-run), §10 (PR slicing). Tell me where you disagree.

### 2. PR2 — `d7a95eb` (2 min)

- `backend/src/modules/ai/primitives/README.md` — policy you can quote at future agents
- `backend/src/modules/ai/primitives/index.ts` — the seam

Trivial diff. Just make sure the policy reads correctly.

### 3. PR5 — `c0239a4` — **the highest-stakes review** (15 min)

This is the architectural piece that replaces the California/US bug.

- `backend/src/modules/ai/schemas/thesis-fit.schema.ts` — the shape that drives every UI fit chip and the screening verdict. **If this shape is wrong, every downstream UI is wrong.**
- `backend/src/modules/ai/prompts/thesis-fit.prompt.ts` — the system prompt. The "HARD RULE: NO STRING EQUALITY" section is the explicit fix for the bug. **Read it carefully — the LLM is going to follow these instructions on every screening decision.**
- `backend/src/modules/ai/agents/thesis-fit/thesis-fit.service.ts` — the call site (single LLM call, structured output, neutral fallback)
- `backend/src/modules/ai/agents/thesis-fit/thesis-fit.spec.ts` — 8 tests, including a literal regression for the California/US case (line ~115-145)

What to look for:
- Does the schema cover the axes you actually need? (Currently: `geography`, `stage`, `sector`, `checkSize`, `overall`, `rationale`.) If you need more axes (e.g., `businessModel`, `traction`), tell me before I wire it.
- Is the prompt's "borderline vs mismatch" guidance calibrated the way you want? I erred on the side of "never use mismatch for missing data" — verify.
- The `neutralFallback` returns `overall: 50`. You may want it to return `0` instead so missing-fit shows up as a problem rather than a neutral score.

### 4. PR6 + PR7 — `e3f75bf`, `492449f` (10 min, in the browser)

Run the frontend with mock auth:

```bash
cd frontend && VITE_MOCK_AUTH=true bun dev
```

Then in browser:

1. `/investor/screening` — top nav shows `Screening 3 | Due Diligence | Contracting | Portfolio`. The Screening tab is highlighted. Three REVIEW/ADVANCE rows with fit chips (`geo stage sector check 62`). Two REJECT rows behind a "Show rejected (2)" toggle.
2. Click "Acme AI" row → modal opens with thesis fit per-axis (geography is MATCH despite being labelled California — that's the regression case), lens scores, missing materials, rationale, PASS / ADVANCE buttons.
3. Click ADVANCE → modal closes, toast appears, row shows `(auto-advanced)`.
4. `/investor` (Pipeline) — same nav at top, DD tab highlighted. The full existing pipeline UI underneath is unchanged.
5. `/investor/contracting` — empty shell with "coming soon" copy.
6. `/investor/portfolio` — your existing portfolio page now has the StageNav on top, Portfolio tab highlighted.

What to look for:
- Is the placement of the StageNav (top of each route, above all existing content) what you want? Or would you prefer it inside the existing `RoleSidebar` as a sidebar group?
- Are the verdict colours (REVIEW=amber, ADVANCE=emerald, REJECT=red) right?
- Is the "Show rejected (N)" toggle in the right place (below the active queue) or should it be a top-level filter?
- The placeholder banners on the screening page — I'd like to keep them until PR4 ships so the client demo doesn't get confused. Yes/no?

## Why I stopped here (PR3, PR4, PR8, PR9)

PR3 (lens-as-agents) and PR4 (screening pipeline backend) are the two heavy backend lifts. They needed:
- A `LensesModule` rewrite (the existing `lens-registry.service.ts` has dependencies I haven't fully mapped)
- New backend route + new BullMQ pipeline + integration with the existing `screening-triage.service.ts`
- Backend tests for the new pipeline composition
- Frontend codegen via `bun generate:api` to expose the new endpoints
- Real-data wiring to replace the placeholder rows in PR6/PR7

Each of those is itself a session of focused work. Doing them in this session would mean shipping unverified code under time pressure — exactly the failure mode that caused the original credibility crisis. So I shipped the architectural artifacts (schema, prompt, UI shell) and stopped.

PR8 (smart re-run) and PR9 (calibration split) are second-order — they only matter once PR4 is alive.

## Blockers I hit

1. **Plan PR1 was wrong.** Resolved by asking you mid-session — but worth flagging that future plans should account for the inventory step. The pattern: "this PR is a simple rename" turns out to be 30+ files including DB migration. Lesson: explore-before-plan.

2. **Plan PR2 was over-spec'd.** It described moving 6900 lines that don't need to move. I made the executive call to shrink it.

3. **Browser-walkthrough of backend changes was not possible in this session.** I have pinchtab + `VITE_MOCK_AUTH=true`, so I verified PR6/PR7 in the browser. But for PR5 (no UI), the verification is unit tests + the LLM call hasn't happened against a real model yet. **You should run the agent against a real model before merging to make sure the prompt produces the shape we want in practice.** A `bun run repl` script that calls `ThesisFitService.assess()` with a real thesis + real startup would do it.

4. **Mock auth uses a fake user.** The mock user shows up as "U User" in the sidebar. That's fine for shell verification but if you want to test thesis-fit against your real thesis data, you need real auth + a real investor record.

5. **Frontend build emits a 2.7MB JS chunk.** Pre-existing issue, not something I introduced. Worth a future PR to split with `manualChunks`. Documented for awareness.

## How to validate this PR set yourself

```bash
# 1. Tests + types pass
cd backend && bunx tsc --noEmit && bun test src/modules/ai/agents/thesis-fit
cd frontend && bunx tsc --noEmit && bun run build

# 2. Browser walkthrough
cd frontend && VITE_MOCK_AUTH=true bun dev
# Open http://localhost:3030/investor/screening, follow checklist in §"What to review" above

# 3. Inspect the new code surfaces
git log --oneline d7a95eb~..HEAD
git diff d7a95eb~..HEAD --stat
```

## What I'd do next session (with your blessing)

Order matters — these are dependency-sorted:

1. **Wire ThesisFitService into the existing DD pipeline** (~30 min). One call site after research completes, stores the result on the deal. This lets you see real LLM-generated fit objects on real deals immediately — no waiting for PR4. The bug stops happening for new DD runs as soon as this lands.

2. **PR3 — lens-as-agents** (~2 hours). Delete `lens-registry.service.ts`, reimplement the three lenses as siblings of the research agents. Verify DD pipeline produces equivalent lens scores via a fixture comparison.

3. **PR4 — Screening pipeline backend** (~3 hours). Compose primitives + lenses (light depth) + thesis-fit + triage into one BullMQ-driven flow. Add a new `/screening/$startupId` endpoint that returns `ScreeningOutputV2`. Wire it to the gate at `pipeline.service.ts:1983`.

4. **Replace placeholder rows in PR6/PR7 with the real endpoint.** Strip the amber preview banners.

5. PR8, PR9 — defer until the above stabilises and produces signal.

## Honesty checks

- I did not break anything (`tsc --noEmit` clean on both ends, build green, no migrations, no schema changes).
- I did not commit any "✅ done" claim that isn't backed by the diff.
- The work is on `dev`, not pushed. Push when you're satisfied.
- I made 3 plan deviations (PR1 drop, PR2 shrink, PR5 not-yet-wired). Each is in the body of this doc and was a judgement call to keep blast radius small. If you disagree with any of them, tell me which.

baarakAllahu feek. Smaller progress with verifiable trust beats the alternative.
