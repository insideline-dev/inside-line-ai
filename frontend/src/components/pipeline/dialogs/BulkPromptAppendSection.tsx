interface BulkPromptAppendSectionProps {
  orchestratorId: string;
}

// Feature removed: bulk append endpoint no longer exists on the backend.
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function BulkPromptAppendSection(_props: BulkPromptAppendSectionProps) {
  return null;
}
